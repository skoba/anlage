# Claude Code 実装プロンプト：セマンティックパスカード基盤（意味索引）

**対象リポジトリ**: Anlage
**作成日**: 2026-08-19
**位置づけ**: 11月12日 医療情報学連合大会チュートリアルデモ、および12月世界公開に向けた基盤モジュール第1弾

---

## 0. ミッション

Anlageに「セマンティックパスカード」層を実装する。OPTが実行時に投入された瞬間、その全ノードから意味情報カード（パスカード）を抽出し、検索可能な意味索引を構築する。索引は静的成果物ではなく、**OPT投入によって実行時に育つ層**である（「クリニカルモデルを置けば動くEHR」の思想の延長）。

この索引は以下の消費者の共通基盤となる。**本プロンプトの範囲は索引そのものまで**であり、消費者側は別プロンプトで指示する：

1. 帳票→テンプレート変換パイプライン（10月・別プロンプト）
2. NL→AQL（11月・別プロンプト）
3. 将来: ambient capture、GDL2オーサリング支援、AIモデリング支援（2027年）

## 1. 文脈（作業前に必読）

- 4層構成: openehr-ruby → openehr-rails → openehr-rails-startup → **Anlage**（本リポジトリ、参照実装）
- Anlageには既にOPTドロップゾーン（OPT投入→動的フォーム生成→検証→保存→FHIR R5ファサード）がある。本モジュールはこの実行時パイプラインにフックする
- 締切: **11月5日デモビルド凍結／11月12日デモ本番**。本基盤は**9月末までにWP4完了**が目標（10月に帳票パイプラインがこの上に載るため）
- パスカードは「構造の意味論（アーキタイプ）」と「語彙の意味論（ターミノロジー）」の結節点である。読み（検索・AQL）と書き（帳票・音声）の両方向を同一カードでグラウンディングする設計

## 2. 絶対規律

本リポジトリの絶対規律（8項目: 探索→計画→承認→実装 / No API guessing / 実物主義 / TDD（t-wada方式） / 層規律 / コンテンツ防火壁 / LLM分業原理 / スコープ規律）は、ルートの `CLAUDE.md` を唯一の正本として定める。Claude Codeセッションでは自動で読み込まれるが、本プロンプトを単独で参照する場合も必ず `CLAUDE.md` を先に読むこと。違反したらその場で停止して報告する。

本WPで特に効く適用点：

- 規律2（No API guessing）→ WP0の全調査項目にソース引用を付ける
- 規律3（実物主義）→ WP1のサンプルカードとWP2以降の全テストフィクスチャは実OPT由来のみ
- 規律4（TDD）→ 各WP計画のテストTODOリストが最初の成果物。埋め込み対象はOPT由来テキストのみ（規律6・7）

## 3. ワークパッケージ

### WP0: 探索レポート（コード変更なし）

- Anlageの現構成を調査する: OPT取り込み経路、実行時レジストリ、ドロップゾーンのフック可能点、DB構成（PostgreSQLバージョン、pgvector導入可否）、Ruby/Railsバージョン、テスト環境（RSpec導入状況・spec構成・実行方法。未導入なら導入計画をWP2計画に含める）
- openehr-ruby / openehr-rails のOPTパーサが露出する情報を棚卸しする: ノード列挙手段、アーキタイプパス、at-code、言語別ラベル・説明（**日本語の取得可否を必ず確認**）、RM型、occurrences、データ型制約（単位・値域・精度・値集合）、用語バインディング
- すべての項目にソース引用（ファイルパス＋コード）を付ける
- パスカードに必要だが取得手段が未確認・欠落の項目を列挙する（対応方針の判断は人間が行う）
- **成果物**: `docs/design/pathcards-wp0-exploration.md`
- **提出して停止。承認を待つ**

### WP1: スキーマ設計文書＋サンプルカード3枚（コード変更なし）

パスカード・スキーマ v1 を設計文書として提案する。必須ブロック：

- **identity**: template_id / archetype_id / path / at_code
- **semantics**: rm_type / labels（言語別、日本語優先）/ descriptions（言語別）
- **constraints**: occurrences / データ型別制約（単位・値域・精度・値集合）
- **bindings**: `[{system_uri, code, display}]` の配列（用語バインディング三つ組。空を許容）
- **capture**: 帳票正規化規則の構造定義。和暦→ISO日付、全角数字、複合値分解（例: 血圧「120/80」）、チェックボックス群→値集合、「その他（　）」→コード＋自由記載の混成。**初期値は空でよいが構造は定義する**
- **reserved**: voice_aliases 等、2027年拡張用の予約枠（空）
- **provenance**: 抽出元OPT、抽出日時、抽出器バージョン

サンプルカード3枚を**実OPTから手作業で抽出**して添付する（各値にソース引用を付ける）：

1. problem_diagnosis 系アーキタイプの傷病名要素（コード化要素・bindingsの実例）
2. laboratory_test_result 系アーキタイプの検査値要素（DV_QUANTITY: 単位・基準範囲の実例）
3. openEHR-EHR-OBSERVATION.blood_pressure.v2 の収縮期血圧 **at0004**（本プロジェクトの記念碑。かつて記憶からのat-code記載が誤りを生んだ事例に由来する「at-codeは記憶から書かず実行時にOPTから引く」原則の象徴として、カード内コメントに一行その旨を残すこと）

- **成果物**: `docs/design/pathcards-schema-v1.md`
- **提出して停止。承認を待つ**

### WP2: 抽出器

- OPT→パスカード集合の抽出器を実装し、OPT投入イベントにフックする
- 開発はTDDで進める（ノード種別ごとの振る舞い──量・コード化テキスト・日付・値集合など──をRSpecで先に定義してから実装する）
- 実OPTによるゴールデンテスト（抽出結果スナップショット）は、TDDのユニットスペックの代替ではなく、仕上げの回帰網として最後に追加する
- **DoD**: デモ用OPT群で全ノードのカード化が通る。日本語ラベルが取得できている（取得できないノードは欠落として報告する）

### WP3: 索引と検索

- pgvectorによる埋め込み索引＋構造メタデータ検索。**外部ミドルウェアの新規追加は禁止**（Rails＋PostgreSQL内で完結させる）
- 埋め込み呼び出しは**プロバイダ抽象層**経由で実装する（実装を差し替え可能に。プログラム終了後のローカル／オープンウェイト代替を見据える）
- 内部検索API＋最小の開発用UI（クエリ→上位カード表示）
- 索引はOPT投入時に自動更新されること
- **DoD**: 日本語クエリで関連カードが返る

### WP4: 評価ハーネス

- 「日本語臨床記述→正解アーキタイプ（可能ならパスまで）」形式の評価器。指標は top-1 / top-3 精度
- シード20問を提案する。正解はCKM実在アーキタイプに限る（人間レビューで確定し、コミュニティで100問へ拡充する前提の設計にする）
- CIで実行可能にする
- **DoD**: `rake pathcards:eval` 一発でスコアが表示される

### WP5: terminologyモジュール

- 名前空間分離（例: `Anlage::Terminology`）。**将来の別サービス切り出しを前提にした境界設計**とする（インターフェースを内部サービスオブジェクトに集約し、in-process→out-of-processの移行が呼び出し側に波及しない形）
- 内部サービスとして FHIR terminology API 相当の3操作: `$lookup`（コード→日本語表示名）/ `$validate-code`（値集合所属判定）/ `$expand`（値集合展開）
- ICD10対応標準病名マスター・臨床検査マスター（JLAC10系）の**ローダー**を実装する。マスターデータは非同梱とし、取得手順を `docs/terminology-setup.md` に記載する
- 設計文書に**防火壁条項**を明記する: (a) コンテンツ非同梱の原則 (b) 施設・プロジェクトライセンス系コンテンツ（SNOMED CT等）を扱う時点で本モジュールを別プロセス・別リポジトリ（openehr-termserv-rails）へ昇格させる、という抽出条件
- **DoD**: 開発用の小規模抜粋データ（人間が手元投入）で lookup / validate が通り、パスカードの bindings から解決できる

## 4. スコープ外（今回は実装しない）

帳票ドロップゾーン本体／NL→AQL／SNOMED CT・ECL・包摂推論／OET→OPTコンパイラ／音声処理／UIの作り込み。これらは別プロンプトで指示する。着想は `docs/ideas-2027.md` へ。

## 5. 進行ルール

- 最初のターンでは **WP0のみ** を実施して停止する
- 以降、各WPの冒頭で計画（対象ファイル・**テストTODOリスト**・コミット分割案）を提示し、承認後に最初のRedから実装へ進む。テストTODOリストはt-wada方式の最初の成果物であり、計画承認の審査対象に含める
- すべての報告の末尾に「未確認事項」と「承認が必要な判断」を明示する
- 期日感: WP0–1＝8月中、WP2–4＝9月、WP5＝9月末〜10月上旬